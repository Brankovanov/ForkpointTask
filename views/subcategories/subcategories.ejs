<%- include ../header.ejs %>
    <div class="subcategories-wrapper">
        <div class="container-fluid">
            <div class="container">
                <nav aria-label="breadcrumb ">
                    <ol class="breadcrumb bg-white px-0">
                        <li class="breadcrumb-item active" aria-current="page">
                            <a href="/">Home</a>
                        </li>
                        <% results[0].primary_category_id
                        .split('-').forEach(element => {%>
                            <li class="breadcrumb-item active" aria-current="page">
                                <a href="/categories/:<%=element%>">
                                    <% let name = element.charAt(0).toUpperCase() + element.slice(1) %>
                                        <%=name%>
                                </a>
                            </li>
                            <% });%>
                    </ol>
                </nav>
            </div>
        </div>
        <div class="container-fluid">
            <div class="container">
                <div class="row row-cols-1 row-cols-md-2">
                    <% results.forEach((test) => { %>
                        <div class="col mb-4 ">
                            <div class="card border-dark rounded-lg h-100 d-inline-block pb-5">
                                <div class="card-body h-100 d-inline-block">
                                    <h5 class="card-title h-25">
                                        <%= test.name %>
                                    </h5>
                                    <div class="row h-50">
                                        <div class="col-4">
                                            <% for(let element of test.image_groups){ %>
                                                <% for(let el of element.images){ %>
                                                    <img class="card-img-top" src="/images/<%=el.link%>" alt="Card image cap">
                                                    <% break;%>
                                                        <% }%>
                                                            <% break;%>
                                                                <% }%>
                                        </div>
                                        <div class="col-8">
                                            <p>
                                                <%= test.short_description %>
                                            </p>
                                        </div>
                                    </div>
                                    <div class="row h-25 pt-5">
                                        <div class="col">
                                            <%= test.price %>
                                                <%= test.currency %>
                                        </div>
                                        <div class="col d-flex justify-content-end">
                                            <a href="/products/:<%= test.id%>" class="btn btn-primary">View more</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <%});%>
                </div>
            </div>
        </div>
        <%- include ../footer.ejs %>